connect to db;

define ss as s s | epsilon;
define a as b*+?epsilon;

get edges
    where (a, b, _) satisfy (is_start a or is_final a) and not is_start b
    from graph & (epsilon | ss);

get count of edges from graph with starts as [1, 2, 3, 57] and finals as [1..43]
